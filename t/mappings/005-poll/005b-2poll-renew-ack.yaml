template: poll-ack.tt

vars:
  msg_id: 0100Test /tmp/srsregn-regression//submodules/SRS-EPP-Proxy/t/mappings/005-poll/create/005b-2-renew.yml 0

input_assertions:
  class: SRS::EPP::Command::Poll
  match:

SRS:
 - assertions:
     count:
     attr_is:
output_assertions:
  attr_is:
    - [ '//epp:msgQ', 'count', 3, 'EPP: count of remaining messges' ]
    - [ '//epp:result', 'code', 1000, 'EPP: response code' ]
