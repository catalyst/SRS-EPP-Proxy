template: delete-contact.tt

vars:
  contact_id: c_id2

input_assertions:
  match:

SRS:
  - assertions:
      attr_is:

output_assertions:
  count:
    - [ '//epp:resData', 0, 'EPP: No resData in the response' ]
  match:
  attr_is:
    - [ '//epp:result', 'code', 2306, 'EPP: Unexpected failure' ]
